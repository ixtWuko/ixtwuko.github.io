<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://ixtwuko.github.io/</id>
    <title>Indie Moments</title>
    <updated>2020-01-07T02:36:20.429Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://ixtwuko.github.io/"/>
    <link rel="self" href="https://ixtwuko.github.io/atom.xml"/>
    <subtitle>止步不前</subtitle>
    <logo>https://ixtwuko.github.io/images/avatar.png</logo>
    <icon>https://ixtwuko.github.io/favicon.ico</icon>
    <rights>All rights reserved 2020, Indie Moments</rights>
    <entry>
        <title type="html"><![CDATA[Gridea 博客环境恢复]]></title>
        <id>https://ixtwuko.github.io/post/gridea-bo-ke-huan-jing-hui-fu</id>
        <link href="https://ixtwuko.github.io/post/gridea-bo-ke-huan-jing-hui-fu">
        </link>
        <updated>2020-01-07T02:22:56.000Z</updated>
        <summary type="html"><![CDATA[<ol>
<li>安装 git 与 Gridea。</li>
<li>GitHub &gt; Settings &gt; Develop settings &gt; Personal access tokens 中，会有以前创建的关于 Gridea 的 token，但是已经无法查看。如果以前有保存在其它地方，找到这个 token；否则只能重新生成。</li>
<li>进入 Gridea 设置，设置站点源文件目录，站点源文件可以使用云盘同步。</li>
<li>进入 Gridea 远程设置，基础配置中除CNAME外均需设置，评论配置可根据需要设置。</li>
<li>进入主题设置，确认主题设置无误。</li>
<li>同步一次测试设置是否正确，之后就可以进行其它博客的操作了。</li>
</ol>
]]></summary>
        <content type="html"><![CDATA[<ol>
<li>安装 git 与 Gridea。</li>
<li>GitHub &gt; Settings &gt; Develop settings &gt; Personal access tokens 中，会有以前创建的关于 Gridea 的 token，但是已经无法查看。如果以前有保存在其它地方，找到这个 token；否则只能重新生成。</li>
<li>进入 Gridea 设置，设置站点源文件目录，站点源文件可以使用云盘同步。</li>
<li>进入 Gridea 远程设置，基础配置中除CNAME外均需设置，评论配置可根据需要设置。</li>
<li>进入主题设置，确认主题设置无误。</li>
<li>同步一次测试设置是否正确，之后就可以进行其它博客的操作了。</li>
</ol>
<!-- more -->
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Linux-or-WSL-环境搭建]]></title>
        <id>https://ixtwuko.github.io/post/linux-or-wsl-huan-jing-da-jian</id>
        <link href="https://ixtwuko.github.io/post/linux-or-wsl-huan-jing-da-jian">
        </link>
        <updated>2020-01-07T01:59:55.000Z</updated>
        <summary type="html"><![CDATA[<h2 id="一-系统设置">一 系统设置</h2>
<h3 id="ubuntu系统安装">Ubuntu系统安装</h3>
<p>无需大规模计算的任务场景建议使用虚拟机或WSL。<br>
对于直接安装，这里简要叙述Ubuntu + Windows的安装方法，安装顺序为Windows &gt; Ubuntu，在支持UEFI的PC上可行。</p>
]]></summary>
        <content type="html"><![CDATA[<h2 id="一-系统设置">一 系统设置</h2>
<h3 id="ubuntu系统安装">Ubuntu系统安装</h3>
<p>无需大规模计算的任务场景建议使用虚拟机或WSL。<br>
对于直接安装，这里简要叙述Ubuntu + Windows的安装方法，安装顺序为Windows &gt; Ubuntu，在支持UEFI的PC上可行。</p>
<!-- more -->
<ol>
<li>下载Ubuntu镜像并制作U盘启动盘。在Windows下可以使用Rufus等软件将镜像刻录到U盘，在Ubuntu下可使用如下方法：<pre><code class="language-bash"># find your USB flash memory's path 
$ sudo fdisk -l
# write the iso file into USB flash memory 
$ sudo dd in=/path/to/image.iso of=/dev/sdx
</code></pre>
</li>
<li>为Ubuntu准备磁盘空间。在Windows下使用系统自带的分区工具，为Ubuntu的安装整理出一整块的空间。</li>
<li>重启并设置U盘为启动项，开始Ubuntu系统的安装。建议的分区方式如下，主分区与home分区的大小可以根据实际情况调整，主分区中占据空间的是软件，home分区中占据空间的个人软件和数据。<pre><code class="language-bash">/     ext4 150G 
swap  swap #depend on the size of memary
/boot ext4 512M 
/home ext4 #remain
# 8G内存可以划分4G的swap，16G内存可以划分其1/3大小的swap
# 根据实际需要，可以将一些可能会很大的文件夹单独分区
/var ext4
/usr ext4
</code></pre>
</li>
<li>选择不同的系统引导方式，如果希望直接引导windows，将引导设置到boot分区。</li>
</ol>
<p>注意：删除Ubuntu时，不能直接删除分区，直接删除会导致错误的引导项。如果引导设置在boot分区，除删除所有的ubuntu分区外，还需删除esp分区中的Ubuntu文件夹，以及使用bootice删除Ubuntu的启动项。</p>
<h3 id="更换软件源并更新">更换软件源并更新</h3>
<ol>
<li>为Ubuntu设置国内源：<pre><code class="language-bash"># backup and modify source.list 
$ cp /etc/apt/source.list /etc/apt/source.list.backup 
$ sudo vim /etc/apt/source.list 
# add aliyun source 
:0,$ s/cn.archive.ubuntu.com/mirrors.aliyun.com/
:wq
</code></pre>
</li>
<li>更新系统<pre><code class="language-bash">$ sudo apt update &amp;&amp; sudo apt upgrade -y
</code></pre>
</li>
</ol>
<h3 id="更改系统时间">更改系统时间</h3>
<ol>
<li>在安装Ubuntu + Windows后，可能会出现某个系统的时间不对的情况，是由于Ubuntu将机器时间作为UTC时间，而Windows将机器时间作为本地时间导致的，可以手动进行如下修改并重启。<pre><code class="language-bash">$ timedatectl set-local-rtc 1
</code></pre>
</li>
</ol>
<h3 id="安装其它语言">安装其它语言</h3>
<ol>
<li>以中文语言包为例。<pre><code class="language-bash">$ sudo apt install language-pack-zh-hans language-pack-zh-hant
</code></pre>
</li>
</ol>
<h3 id="安装ssh">安装ssh</h3>
<ol>
<li>使用openssh<pre><code class="language-bash">$ apt install openssh-server
$ service ssh start
</code></pre>
</li>
</ol>
<h3 id="替换shell">替换Shell</h3>
<ol>
<li>
<p>oh-my-zsh</p>
<pre><code class="language-bash"># install zsh and change default shell to zsh 
$ apt install zsh 
$ chsh -s /usr/bin/zsh 
# install oh-my-zsh 
$ sh -c &quot;$(wget -O- https://raw.githubusercontent.com/robbyrussell/oh-my-zsh/master/tools/install.sh)&quot; 
# change theme of oh-my-zsh 
$ vim ~/.zshrc 
-------------------- 
.zshrc 
ZSH_THEME=&quot;ys&quot; 
-------------------- 
$ source ~/.zshrc
</code></pre>
</li>
<li>
<p>fish shell</p>
<pre><code class="language-bash"># install fish and change default shell to fish 
$ apt install fish 
# open a web server for fish config 
$ fish_config
</code></pre>
</li>
</ol>
<h3 id="处理数据盘">处理数据盘</h3>
<ol>
<li>Ubuntu无法像Windows一样自动挂载所有的数据盘，数据盘挂载方法如下：<pre><code class="language-bash"># show the device and find the data disk, for example /dev/vdb
$ fdisk -l
# partition
$ fdisk /dev/vdb
# formating
$ mkfs.ext4 /dev/vdb1
# add the partition info into system
$ echo /dev/vdb1 /data ext4 defaults 0 0 &gt;&gt; /etc/fstab # you can change /data to other file such like /mnt
# mount
$ mount -a
</code></pre>
</li>
</ol>
<h3 id="免密登录">免密登录</h3>
<ol>
<li>远程服务器的密码登录存在被暴力破解的风险，即使设置复杂的密码，服务器仍会被频繁尝试登录而消耗计算资源。对于远程服务器建立使用私钥登录。<pre><code class="language-bash"># upload authorized_key file to the server.
$ scp local/path/of/authorized_keys root@server_address:/root/.ssh/
# modify the config
$ sudo vim /etc/ssh/sshd_config
--------------------
# sshd_config
RSAAuthentication yes
PubkeyAuthentication yes
AuthorizedKeysFile .ssh/authorized_keys
--------------------
# restart ssh service and test
$ sudo service ssh restart
# if system don't have ssh server, install it using
$ sudo apt install openssh-server
# disable password login
$ sudo vim /etc/ssh/sshd_config
--------------------
# sshd_config
PasswordAuthentication no
ChallengeResponseAuthentication no
--------------------
# restart ssh service and test
$ sudo service ssh restart
</code></pre>
</li>
</ol>
<h3 id="跨大版本升级">跨大版本升级</h3>
<ol>
<li>Ubuntu的版本优先选择LTS即长期支持的版本，系统安装多年后需要更新大版时，可以使用如下命令更新。<pre><code class="language-bash">$ do-release-upgrade 
# if you want to upgrade to the latest non-LTS develoment release, 
# set Prompt=normal in /etc/update-manager/release-upgrades.
</code></pre>
</li>
</ol>
<h2 id="二-编译环境">二 编译环境</h2>
<p>这里的编译环境主要是以Cpp和Python为主。</p>
<h3 id="cpp-环境">Cpp 环境</h3>
<ol>
<li>
<p>安装一些常用的软件</p>
<pre><code class="language-bash"># git, wget, curl, unzip, vim 
$ sudo apt install git wget curl unzip vim 
# gcc g++ make ... cmake clang 
$ sudo apt install build-essential cmake clang 
# change default compiler to clang 
# run these command and choose the clang 
$ sudo update-alternatives --config cc 
$ sudo update-alternatives --config c++
</code></pre>
</li>
<li>
<p>安装IDE如CLion(付费)，或者使用Visual Studio Code + CMake。</p>
</li>
<li>
<p>安装需要的第三方库</p>
</li>
</ol>
<h3 id="python环境">Python环境</h3>
<ol>
<li>下载Anaconda并安装，推荐使用conda管理package，当conda中没有某个package时，使用工作虚拟环境中的pip安装该package，不推荐使用系统默认的pip安装package。</li>
<li>某些软件可能在编译过程中调用python，可以为系统单独安装python。</li>
<li>安装IDE，推荐PyCharm。</li>
</ol>
<h2 id="三-其它软件">三 其它软件</h2>
<ul>
<li>在有桌面环境的Ubuntu中，SSR梯子推荐使用electron-SSR。</li>
<li>在有桌面环境的Ubuntu中，数据备份推荐使用timeshift。</li>
</ul>
]]></content>
    </entry>
</feed>